{% extends "layout.html.twig" %}

{% block title -%}
    {{ git_repository_name(commit) }} {{ commit.subjectMessage}} ({{ commit.shortHash }})
{%- endblock %}

{% block content %}
    {{ git_commit_header(commit) }}
    {{ git_diff(commit.diff) }}
{% endblock %}
