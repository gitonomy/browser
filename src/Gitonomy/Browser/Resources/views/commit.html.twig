{% extends "layout.html.twig" %}

{% block title -%}
    {{ commit.repository.name }} {{ commit.subjectMessage}} ({{ commit.shortHash }})
{%- endblock %}

{% block content %}
    {{ git_commit(commit) }}
    {{ git_diff(commit.diff) }}
{% endblock %}
