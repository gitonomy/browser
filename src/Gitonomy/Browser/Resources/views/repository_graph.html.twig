{% extends "layout.html.twig" %}

{% block content %}
    <h2>{{ name }}</h2>
    {{ git_log(repository.log.setOffset(5).setLimit(20), {query_url: path('repository_ajaxLog', {name: name})}) }}
{% endblock %}
